// Package auth establishes caller identity: it verifies the Better Auth JWT
// and resolves the session and permissions behind it.
//
// Signup, login, and password hashing belong to Better Auth in the frontend.
package auth
