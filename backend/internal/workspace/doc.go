// Package workspace owns workspaces, their membership, and the invitations
// that grant it.
//
// It decides who belongs where and with what role; what a role may then do is
// the auth package's table.
package workspace
